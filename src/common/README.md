# @credenza3/partner-sdk/common

## USAGE

```
import { requestPassportIdSignature } from '@credenza3/partner-sdk/common'

const { signature } = await requestPassportIdSignature(sub: string, message: string)
```
